<div class="container-fluid text-center">
  <div class="form">
    <h1> Buy Stock </h1>
    <h2> <%= @stock_info['Name'] %> (<%= @stock_info['symbol'] %>) </h2> <br>
      Price: <%= @stock_info['l'] %>

    <%= form_for @stock, :url => buy_path(@stock_info['symbol']) do |f| %>
      <p>
        Portfolio: <br>
        <%= f.select(:portfolio_id, @portfolios) %>
      </p>

      <p>
        <%= f.label :volume %><br>
        <%= f.number_field :volume %>
      </p>

      <%= f.hidden_field(:symbol, value: @stock_info['symbol']) %>
      <%= f.hidden_field(:buy_price, value: @stock_info['l'].delete(',')) %>

      <p>
        <%= f.submit "Buy Stock" %>
      </p>
    <% end %>

    <%= link_to 'Back', stock_path(@stock_info['symbol']) %>

  </div>
</div>
